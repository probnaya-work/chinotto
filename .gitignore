# Dependencies
node_modules/
.pnpm-store/

# Build
dist/
/target/
*.tsbuildinfo

# Tauri
src-tauri/target/
src-tauri/gen/
src-tauri/.fastembed_cache/

# Firebase CLI (local cache; not project config)
.firebase/

# Env
.env
.env.local
*.local

# OS
.DS_Store
Thumbs.db

# Local store / installer output (not committed)
builds/

# Apple API key and release env (never commit)
*.p8
scripts/apple-release-env.sh
scripts/mas-testflight-env.sh
src-tauri/updater.key
src-tauri/updater.key.pub
src-tauri/*.provisionprofile

# Local-only (not in repo)
docs/pre-release-checklist.md
docs/release-macos.md
docs/release-process.md
docs/macos-testflight.md

# Maintainer specs — not published (local only)
docs/internal/

# PR drafting notes (local-only)
.github/pull_request_body*.md

# Local Claude Code runtime state (dev server config, per-machine).
.claude/
